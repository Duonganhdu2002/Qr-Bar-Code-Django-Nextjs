"use client"; 

import React, { useState } from 'react';

const page = () => {

    const [data, setData] = useState([]);

    return (
        <div>
            qr code
        </div>
    );
};

export default page;