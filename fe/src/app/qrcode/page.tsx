"use client"; 

import React, { useState } from 'react';

const page = () => {

    const [data, setData] = useState([]);

    return (
        <div>
            Generate a QR Code
        </div>
    );
};

export default page;